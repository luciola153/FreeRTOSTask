# daplink.cfg
source [find interface/cmsis-dap.cfg]
transport select swd
source [find target/stm32f4x.cfg]

# 关键：设置复位方式为 srst_only（STM32F4 支持硬件复位）
reset_config srst_only

# 降低速度提高稳定性（可选）
adapter speed 1000